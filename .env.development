# When using `yarn start` (with --proxy flag), the platform starts a proxy on localhost:8080
# that forwards to the real DHIS2 server and rewrites cookies for local dev.
DHIS2_BASE_URL=http://localhost:8080
